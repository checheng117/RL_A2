# SFT loss summary

Parsed from `outputs/checkpoints/sft_full_3090/checkpoint-273/trainer_state.json`.

- **Training loss** rows come from `log_history` entries that include `loss` (no `eval_loss` on the same row).
- **Eval loss** rows come from entries that include `eval_loss` (end-of-epoch evaluations; different logging frequency than train).

- Steps logged (train): **27**; eval points: **3**.
- Last training loss (final log): **0.2866**
- Last eval loss: **0.8997**

Figure: `outputs/report_assets/sft_loss_curves.png`
CSV: `outputs/report_assets/sft_loss_history.csv`

